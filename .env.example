# Optional public Formspree endpoint. Leave empty to keep the demo form disabled.
NEXT_PUBLIC_DEMO_FORM_ENDPOINT=

# Optional GitHub Pages repository subpath for local build testing.
# The deployment workflow sets this automatically.
NEXT_PUBLIC_BASE_PATH=
